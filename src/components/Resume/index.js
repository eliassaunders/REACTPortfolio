import React from 'react'

function Resume() {
   return (
      <div>
         <button>
         <a download="resume(1).pdf">Download Resume</a>
         </button>
      </div>
   )
}

export default Resume;